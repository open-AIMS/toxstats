% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parametric.R
\name{dunnett}
\alias{dunnett}
\title{Dunnett's procedure}
\usage{
dunnett(x, ..., alpha = 0.05)
}
\arguments{
\item{x}{A \code{toxcalc_data()} object, or a data frame that can be coerced to
one by passing it through \code{toxcalc_data()}.}

\item{...}{Additional arguments passed to \code{toxcalc_data()} when \code{x} is a
bare data frame, and otherwise unused.}

\item{alpha}{Significance level for the hypothesis tests themselves. The EPA
manuals specify 0.05, one-sided.}
}
\value{
An object of class \code{toxcalc_comparison}, a list with elements
\code{test}, \code{method}, \code{comparisons} (a data frame with one row per treatment
concentration), \code{noec}, \code{loec}, \code{monotone}, \code{critical}, \code{sw}, \code{df},
\code{alpha} and \code{reference}.
}
\description{
Compares each concentration mean with the control mean, holding the overall
error rate at \code{alpha} across all the comparisons. This is the parametric
test the EPA flowchart selects when the residuals are normal, the variances
are homogeneous, and every concentration has the same number of replicates.
}
\details{
The statistic for concentration \code{i} is

\deqn{t_i = \frac{\bar{Y}_1 - \bar{Y}_i}{S_w \sqrt{1/n_1 + 1/n_i}}}

with \code{Sw} the square root of the within mean square from a one-way analysis
of variance. It is compared with a one-sided critical value computed as
described in \code{\link[=msd]{msd()}}; the manual reads the same value from a table credited
to Miller (1981).

Dunnett's procedure requires equal replication. When replication is unequal
the manual directs the analysis to \code{\link[=bonferroni_t]{bonferroni_t()}} instead, and this
function signals an error rather than silently returning a value the manual
would not accept.
}
\examples{
# Appendix C worked example; the manual reports NOEC 128 and LOEC 256
dunnett(fathead_c1, response = "weight")

}
\references{
US EPA (2002) EPA-821-R-02-013, Appendix C.

Dunnett CW (1955) A multiple comparison procedure for comparing several
treatments with a control. \emph{Journal of the American Statistical
Association} 50:1096-1121.
}
