% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parametric.R
\name{bonferroni_t}
\alias{bonferroni_t}
\title{The t test with Bonferroni's adjustment}
\usage{
bonferroni_t(x, ..., alpha = 0.05)
}
\arguments{
\item{x}{A \code{tox_data()} object, or a data frame that can be coerced to
one by passing it through \code{tox_data()}.}

\item{...}{Additional arguments passed to \code{tox_data()} when \code{x} is a
bare data frame, and otherwise unused.}

\item{alpha}{Significance level for the hypothesis tests themselves. The EPA
manuals specify 0.05, one-sided.}
}
\value{
An object of class \code{tox_comparison}, a list with elements
\code{test}, \code{method}, \code{comparisons} (a data frame with one row per treatment
concentration), \code{noec}, \code{loec}, \code{monotone}, \code{critical}, \code{sw}, \code{df},
\code{alpha} and \code{reference}.
}
\description{
Compares each concentration mean with the control mean, dividing the
significance level by the number of comparisons. This is the parametric test
the EPA flowchart selects when the assumptions are met but replication is
unequal, which rules out Dunnett's procedure.
}
\details{
The statistic is the same as in \code{\link[=dunnett]{dunnett()}}; only the critical value
differs, being \code{qt(1 - alpha / k, df)} for \code{k} treatment concentrations.
This reproduces the manual's Table D.5 exactly: 2.510 for \code{alpha = 0.05}
with 14 degrees of freedom and four concentrations, and 2.241 in the row
for infinite degrees of freedom against a printed 2.242.

Bonferroni's adjustment sets an upper bound of \code{alpha} on the overall error
rate rather than fixing it there, so it is more conservative than Dunnett's
procedure and correspondingly less powerful.
}
\examples{
# Appendix D uses the Appendix C data with one replicate lost
lost <- fathead_c1[-19, ]
bonferroni_t(lost, response = "weight")

}
\references{
US EPA (2002) EPA-821-R-02-013, Appendix D.
}
