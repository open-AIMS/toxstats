% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parametric.R
\name{dunn_sidak_t}
\alias{dunn_sidak_t}
\title{The t test with Dunn-Sidak's adjustment}
\usage{
dunn_sidak_t(x, ..., alpha = 0.05)
}
\arguments{
\item{x}{A \code{toxcalc_data()} object, or a data frame that can be coerced to
one by passing it through \code{toxcalc_data()}.}

\item{...}{Additional arguments passed to \code{toxcalc_data()} when \code{x} is a
bare data frame, and otherwise unused.}

\item{alpha}{Significance level for the hypothesis tests themselves. The EPA
manuals specify 0.05, one-sided.}
}
\value{
An object of class \code{toxcalc_comparison}, a list with elements
\code{test}, \code{method}, \code{comparisons} (a data frame with one row per treatment
concentration), \code{noec}, \code{loec}, \code{monotone}, \code{critical}, \code{sw}, \code{df},
\code{alpha} and \code{reference}.
}
\description{
As \code{\link[=bonferroni_t]{bonferroni_t()}}, but using Dunn-Sidak's adjustment, which is exact when
the comparisons are independent and so slightly less conservative than
Bonferroni's.
}
\details{
The critical value is \code{qt(1 - (1 - (1 - alpha)^(1 / k)), df)}. ToxCalc
offered this test; the EPA flowchart does not select it, so using it is a
documented departure from the manual.
}
\examples{
dunn_sidak_t(fathead_c1, response = "weight")

}
\references{
Sidak Z (1967) Rectangular confidence regions for the means of multivariate
normal distributions. \emph{Journal of the American Statistical Association}
62:626-633.
}
