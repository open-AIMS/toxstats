% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nonparametric.R
\name{steel}
\alias{steel}
\title{Steel's Many-One Rank Test}
\usage{
steel(
  x,
  ...,
  alpha = 0.05,
  method = c("asymptotic", "simulated", "exact"),
  nsim = 10000
)
}
\arguments{
\item{x}{A \code{toxcalc_data()} object, or a data frame that can be coerced to
one by passing it through \code{toxcalc_data()}.}

\item{...}{Additional arguments passed to \code{toxcalc_data()} when \code{x} is a
bare data frame, and otherwise unused.}

\item{alpha}{Significance level for the hypothesis tests themselves. The EPA
manuals specify 0.05, one-sided.}

\item{method}{How the p-value is obtained: \code{"asymptotic"} (the default),
\code{"simulated"}, or \code{"exact"}. Passed to \code{\link[kSamples:Steel.test]{kSamples::Steel.test()}}.}

\item{nsim}{Number of simulations when \code{method = "simulated"}.}
}
\value{
An object of class \code{toxcalc_comparison}, a list with elements
\code{test}, \code{method}, \code{comparisons} (a data frame with one row per treatment
concentration), \code{noec}, \code{loec}, \code{monotone}, \code{critical}, \code{sw}, \code{df},
\code{alpha} and \code{reference}.
}
\description{
Compares each concentration with the control by ranking the two together,
holding the overall error rate at \code{alpha} across all the comparisons. This
is the test the EPA flowchart selects when the residuals are not normal, or
the variances are not homogeneous, and replication is equal.
}
\details{
For each control and concentration pair the observations are combined and
ranked, and the ranks belonging to the concentration are summed. A small
rank sum indicates a response below the control.

The manual compares that rank sum with a tabulated critical value
(Table E.5). This package instead obtains a multiplicity-adjusted p-value
from \code{\link[kSamples:Steel.test]{kSamples::Steel.test()}}, which evaluates the joint null distribution
of all \code{k} comparisons directly. That avoids reproducing a third-party
table, extends beyond the tabulated designs, and reproduces the manual's
conclusion on its own worked example, including the borderline comparison
whose rank sum equals the tabulated critical value exactly.

Steel's test requires equal replication. The manual directs unequal
replication to \code{\link[=wilcoxon_rank_sum]{wilcoxon_rank_sum()}}, and this function signals an error
rather than returning a value the manual would not accept. It also requires
at least four replicates per concentration (section 9.4.5.2).
}
\examples{
# Appendix E worked example. The 50 per cent concentration is excluded from
# the reproduction analysis because survival there was significantly
# reduced; see section 9.5.2 and the Appendix G example.
repro <- ceriodaphnia_e1[ceriodaphnia_e1$conc < 50, ]
steel(repro, response = "young")

}
\references{
US EPA (2002) EPA-821-R-02-013, Appendix E.

Steel RGD (1959) A multiple comparison rank sum test: treatments versus
control. \emph{Biometrics} 15:560-572.
}
