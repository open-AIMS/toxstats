% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tox_data.R
\name{tox_data}
\alias{tox_data}
\alias{print.tox_data}
\alias{summary.tox_data}
\alias{as.data.frame.tox_data}
\title{Prepare toxicity test data for analysis}
\usage{
tox_data(
  data,
  conc = "conc",
  response = "response",
  n_exposed = NULL,
  replicate = NULL,
  type = c("continuous", "quantal"),
  direction = c("decreasing", "increasing"),
  control = 0,
  conc_units = "\%",
  response_units = NULL
)

\method{print}{tox_data}(x, ...)

\method{summary}{tox_data}(object, ...)

\method{as.data.frame}{tox_data}(x, row.names = NULL, optional = FALSE, ...)
}
\arguments{
\item{data}{A data frame with one row per replicate test chamber.}

\item{conc}{Name of the column holding the exposure concentration. A string.}

\item{response}{Name of the column holding the response. For
\code{type = "continuous"} this is the measured value; for \code{type = "quantal"}
it is the number of organisms affected. A string.}

\item{n_exposed}{Name of the column holding the number of organisms exposed
in each replicate. Required when \code{type = "quantal"}, otherwise \code{NULL}.}

\item{replicate}{Name of an optional column labelling replicates within a
concentration, or \code{NULL}.}

\item{type}{The response type. Either \code{"continuous"} (growth, reproduction,
biomass) or \code{"quantal"} (survival, fertilisation, hatching).}

\item{direction}{The expected direction of the concentration-response
relationship for the measured response. Either \code{"decreasing"} (the usual
case, where toxicity reduces the response) or \code{"increasing"}.}

\item{control}{The concentration treated as the control. Defaults to \code{0}.}

\item{conc_units}{Units of \code{conc}, used only for labelling output. A string.}

\item{response_units}{Units of \code{response}, used only for labelling output.
A string, or \code{NULL}.}

\item{x}{A \code{tox_data} object.}

\item{...}{Unused, present for consistency with the generic.}

\item{object}{A \code{tox_data} object.}

\item{row.names}{Unused, present for consistency with the generic.}

\item{optional}{Unused, present for consistency with the generic.}
}
\value{
An object of class \code{tox_data}, a list with elements:
\describe{
\item{\code{replicates}}{a data frame with one row per replicate, holding
\code{conc}, \code{replicate}, \code{response}, and for quantal data \code{n_exposed} and
\code{proportion}.}
\item{\code{pooled}}{a data frame with one row per concentration, holding
\code{n_rep}, \code{mean}, \code{sd} and \code{var} of the analysis response, and for
quantal data the totals \code{n_exposed}, \code{n_affected} and the pooled
\code{proportion}.}
\item{\code{type}, \code{direction}, \code{control}, \code{conc_units}, \code{response_units}}{as
supplied.}
\item{\code{n_dropped}}{the number of rows removed for a missing response.}
}
}
\description{
Validates replicate-level concentration-response data and puts it into the
single form every other function in the package accepts. One row per
replicate test chamber covers both kinds of endpoint the EPA WET manuals
recognise.
}
\details{
The manuals distinguish two response types.

A \strong{continuous} endpoint is a measurement, such as growth in milligrams or
the number of young per female. \code{response} holds the measured value and
\code{n_exposed} is not used.

A \strong{quantal} endpoint is one where each organism either responds or does
not, such as survival, hatching or fertilisation. \code{response} holds the
number of organisms \strong{affected} in the replicate and \code{n_exposed} the number
exposed. Both are required, because the manuals analyse quantal data twice:
the hypothesis-testing flowchart works on replicate-level proportions after
an arc sine square root transformation, while the point-estimation flowchart
works on counts pooled across replicates within a concentration. Recording
the number exposed is what makes both possible from one input.

Rows with a missing response are dropped, and the number dropped is recorded
in the returned object. This matters for the linear interpolation method,
whose bootstrap resamples within a concentration and so depends on how many
replicates each concentration actually has.
}
\section{Methods (by generic)}{
\itemize{
\item \code{print(tox_data)}: Print a compact description of the design.

\item \code{summary(tox_data)}: Return the per-concentration summary.

\item \code{as.data.frame(tox_data)}: Return the validated replicate-level data.

}}
\examples{
growth <- data.frame(
  conc = rep(c(0, 32, 64, 128, 256), each = 4),
  growth = c(
    0.711, 0.662, 0.054, 0.785,
    0.517, 0.501, 0.723, 0.760,
    0.602, 0.669, 0.694, 0.706,
    0.348, 0.400, 0.041, 0.512,
    0.216, 0.277, 0.328, 0.347
  )
)
tox_data(growth, response = "growth")

}
\references{
US EPA (2002) EPA-821-R-02-013, sections 9.4 to 9.6.
}
