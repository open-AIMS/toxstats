% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smooth.R
\name{is_monotone}
\alias{is_monotone}
\title{Is a sequence monotone?}
\usage{
is_monotone(y, direction = c("decreasing", "increasing"))
}
\arguments{
\item{y}{A numeric vector to smooth.}

\item{direction}{The direction the smoothed sequence must follow.
\code{"decreasing"} (the default) returns a non-increasing sequence, as
expected for a response reduced by a toxicant. \code{"increasing"} returns a
non-decreasing sequence, as expected for a proportion responding.}
}
\value{
A list with elements \code{monotone} (a flag), \code{direction} (the direction
tested) and \code{violations} (an integer vector of the positions at which the
sequence departs from the required direction; empty when \code{monotone} is
\code{TRUE}).
}
\description{
Reports whether a sequence already follows the required direction, and where
it first departs from it. Williams' test assumes monotonicity, and the EPA
linear interpolation method smooths towards it, so both need to know.
}
\examples{
is_monotone(c(10, 8, 9, 4), direction = "decreasing")

}
