% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/msd.R
\name{pmsd}
\alias{pmsd}
\title{Percent minimum significant difference}
\usage{
pmsd(
  x,
  ...,
  alpha = 0.05,
  test = c("dunnett", "bonferroni", "sidak"),
  bounds = NULL
)
}
\arguments{
\item{x}{A \code{tox_data()} object, or a data frame that can be coerced to
one by passing it through \code{tox_data()}.}

\item{...}{Additional arguments passed to \code{tox_data()} when \code{x} is a
bare data frame, and otherwise unused.}

\item{alpha}{Significance level for the hypothesis tests themselves. The EPA
manuals specify 0.05, one-sided.}

\item{test}{The multiple-comparison procedure whose critical value is used.
\code{"dunnett"} is the EPA method for a balanced design; \code{"bonferroni"} and
\code{"sidak"} correspond to the t test with Bonferroni's or Dunn-Sidak's
adjustment, used when replication is unequal.}

\item{bounds}{Optional. Either a row name of \link{epa_pmsd_bounds} such as
\code{"fathead_growth"}, or a length-two numeric vector giving the lower and
upper bounds directly. \code{NULL} reports the value without comparison.}
}
\value{
An object of class \code{tox_pmsd}, a list with elements \code{pmsd}
(named by concentration), \code{control_mean}, \code{bounds}, \code{status} and the
underlying \code{msd} object.
}
\description{
The minimum significant difference expressed as a percentage of the control
mean, which is how the EPA method-variability criteria are stated. A value
outside the bounds published for a test method indicates a test that was
either unusually insensitive or unusually sensitive.
}
\details{
\verb{PMSD = 100 * MSD / control mean}.

EPA publishes lower and upper bounds for three sublethal endpoints, shipped
here as \link{epa_pmsd_bounds}. Pass \code{bounds} to compare against one of them.

The manuals require the percent minimum significant difference to be
computed parametrically \strong{even when the flowchart selected a non-parametric
test}, so this function does not consult the flowchart.
}
\examples{
# Appendix C worked example; the manual reports about 24 per cent
pmsd(fathead_c1, response = "weight", bounds = "fathead_growth")

}
\references{
US EPA (2002) EPA-821-R-02-013, section 10.2.8 and Table 6.
}
