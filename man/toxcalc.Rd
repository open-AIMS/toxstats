% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toxcalc.R
\name{toxcalc}
\alias{toxcalc}
\alias{summary.toxcalc}
\alias{as.data.frame.toxcalc}
\title{Run the EPA hypothesis-testing analysis}
\usage{
toxcalc(
  x,
  ...,
  alpha = 0.05,
  alpha_assumption = 0.01,
  test = NULL,
  exclude = NULL,
  pmsd_bounds = NULL,
  branch = c("hypothesis", "both"),
  p = NULL,
  nboot = 200,
  seed = NULL
)

\method{summary}{toxcalc}(object, ...)

\method{as.data.frame}{toxcalc}(x, row.names = NULL, optional = FALSE, ...)
}
\arguments{
\item{x}{A \code{toxcalc_data()} object, or a data frame that can be coerced to
one by passing it through \code{toxcalc_data()}.}

\item{...}{Additional arguments passed to \code{toxcalc_data()} when \code{x} is a
bare data frame, and otherwise unused.}

\item{alpha}{Significance level for the hypothesis tests themselves. The EPA
manuals specify 0.05, one-sided.}

\item{alpha_assumption}{Significance level for the assumption tests
(normality and homogeneity of variance). The EPA manuals specify 0.01.}

\item{test}{Optional. Name of a test to run instead of the one the
flowchart selects: one of \code{"dunnett"}, \code{"bonferroni_t"}, \code{"steel"} or
\code{"wilcoxon_rank_sum"}.}

\item{exclude}{Optional numeric vector of concentrations to exclude from the
hypothesis test, over and above those excluded automatically.}

\item{pmsd_bounds}{Optional. Passed to \code{\link[=pmsd]{pmsd()}}; supplying it also enables
the section 10.2.8.2.5 override.}

\item{branch}{\code{"hypothesis"} (the default) gives the NOEC, LOEC, MSD and
PMSD. \code{"both"} adds a point estimate alongside them, which is what a
laboratory report usually contains: a median lethal concentration through
\code{\link[=lc50]{lc50()}} for a quantal endpoint, or an inhibition concentration through
\code{\link[=icp]{icp()}} for a continuous one.

There is no \code{"point"} option, because \code{\link[=lc50]{lc50()}} and \code{\link[=icp]{icp()}} already provide
that directly and are easier to find. The default is not \code{"both"} because
the point branch runs a bootstrap for a continuous endpoint, which takes
appreciably longer, so it is asked for rather than assumed.}

\item{p}{Percentages for the point branch, or \code{NULL} for the default of
whichever method applies.}

\item{nboot, seed}{Passed to \code{\link[=icp]{icp()}} when the point branch applies to a
continuous endpoint.}

\item{object}{A \code{toxcalc} object.}

\item{row.names}{Unused.}

\item{optional}{Unused.}
}
\value{
An object of class \code{toxcalc}, a list with elements \code{data},
\code{working}, \code{decisions}, \code{assumptions}, \code{comparison}, \code{msd}, \code{pmsd},
\code{noec}, \code{loec}, \code{excluded}, \code{overridden}, \code{transform}, \code{flowchart},
\code{alpha} and \code{alpha_assumption}.
}
\description{
Walks the EPA flowchart, selects the hypothesis test the manual directs for
the data, runs it, and returns the endpoints together with a record of how
the test was chosen. This is the function that recreates what ToxCalc did.
}
\details{
The flowchart is Figure 2 of the chronic manual, which is also Figure 13 of
the acute manual. It branches on four things in order: whether the response
is a proportion, whether the pooled within-group residuals are normal,
whether the variances are homogeneous, and whether replication is equal. The
four terminals are \code{\link[=dunnett]{dunnett()}}, \code{\link[=bonferroni_t]{bonferroni_t()}}, \code{\link[=steel]{steel()}} and
\code{\link[=wilcoxon_rank_sum]{wilcoxon_rank_sum()}}.

Two significance levels are involved and they differ. The assumption tests
use \code{alpha_assumption}, which section 9.4.6.1 sets at 0.01; the hypothesis
test uses \code{alpha}, set at 0.05 and one-sided.
\subsection{What is recorded}{

The value of this function over calling the tests by hand is the decision
trail, returned by \code{\link[=decisions]{decisions()}}. Each row is a branch point: the question,
the criterion, the statistic, the answer, the consequence, and the manual
section that justifies it. \code{summary()} prints it above the endpoints.
}

\subsection{Exclusions}{

Section 9.5.2 excludes some concentrations from the hypothesis test while
retaining them for point estimation. Two rules apply.

A concentration at which every replicate showed a complete response, for
instance total mortality, is excluded automatically for a quantal endpoint.

A concentration at which survival was significantly reduced is excluded from
the analysis of a \strong{sublethal} endpoint such as growth or reproduction.
That requires a separate survival analysis, so it cannot be inferred here;
pass the concentrations to \code{exclude}. The Appendix E example does exactly
this, dropping the 50 per cent concentration from the reproduction analysis
on the strength of the Appendix G survival result.
}

\subsection{The percent minimum significant difference override}{

Section 10.2.8.2.5 states that a concentration shall not be declared toxic
if its relative difference from the control is less than the lower bound
published for the test method. This overrides the hypothesis test. It is
applied only when \code{pmsd_bounds} is supplied, and each concentration it
reverses is added to the decision trail.
}

\subsection{Forcing a test}{

Passing \code{test} runs that test whatever the flowchart selected. When the two
disagree a warning is signalled, \code{overridden} is set, and a row recording
the override is added to the trail, so a forced analysis can never be
printed as though the manual had chosen it.
}
}
\section{Methods (by generic)}{
\itemize{
\item \code{summary(toxcalc)}: Print the decision trail above the endpoints.

\item \code{as.data.frame(toxcalc)}: Return every endpoint as one tidy row.

}}
\examples{
# Appendix C growth data: normal, homogeneous and balanced, so the
# flowchart selects Dunnett's procedure.
fit <- toxcalc(fathead_c1, response = "weight")
summary(fit)

}
\references{
US EPA (2002) EPA-821-R-02-013, Figure 2 and sections 9.4 to 9.6.
}
\seealso{
\code{\link[=decisions]{decisions()}} for the trail, \code{\link[=msd]{msd()}} and \code{\link[=pmsd]{pmsd()}} for sensitivity.
}
