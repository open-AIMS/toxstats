% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assumptions.R
\name{epa_variance}
\alias{epa_variance}
\title{Test the homogeneity of variance assumption}
\usage{
epa_variance(
  x,
  ...,
  method = c("bartlett", "levene", "fligner"),
  alpha_assumption = 0.01
)
}
\arguments{
\item{x}{A \code{toxcalc_data()} object, or a data frame that can be coerced to
one by passing it through \code{toxcalc_data()}.}

\item{...}{Additional arguments passed to \code{toxcalc_data()} when \code{x} is a
bare data frame, and otherwise unused.}

\item{method}{Which test to use. \code{"bartlett"} is the EPA method and the
default.}

\item{alpha_assumption}{Significance level for the assumption tests
(normality and homogeneity of variance). The EPA manuals specify 0.01.}
}
\value{
An object of class \code{toxcalc_htest}, a list with elements \code{method},
\code{statistic}, \code{statistic_name}, \code{p_value}, \code{critical}, \code{alpha}, \code{df},
\code{homogeneous}, \code{conclusion} and \code{reference}.
}
\description{
Tests whether the variance of the response is the same at every
concentration, the second branch point of the EPA hypothesis-testing
flowchart. Dunnett's procedure pools the variance across concentrations, so
it is only valid when they are equal; failing this test sends the analysis
to the non-parametric branch.
}
\details{
Bartlett's test is what the manuals specify (Appendix B, section 3), and is
the default here. It is computed by \code{\link[stats:bartlett.test]{stats::bartlett.test()}}, which uses the
same formula the manuals set out, including the \code{C} correction factor.

Two robust alternatives are offered because Bartlett's test is sensitive to
departures from normality, which is awkward given it is applied immediately
after a normality test that the data may only just have passed. Levene's
test is an analysis of variance on absolute deviations from the group
median, and the Fligner-Killeen test is its rank-based counterpart. Neither
is an EPA method, and using one is recorded as a departure.

Note that the manual's own printed Bartlett statistic for the Appendix B
example, \code{B = 7.691}, is not reproducible from the raw data, which give
\code{6.836}. The difference arises because the manual computes the statistic
from its printed table of variances rounded to four decimal places, one of
which (\code{0.0020} for a variance of \code{0.002055}) is itself rounded incorrectly.
Both values lead to the same conclusion. This package computes from the
unrounded data.
}
\examples{
# Appendix B worked example; the variances are not significantly different.
epa_variance(fathead_b1, response = "weight")

}
\references{
US EPA (2002) EPA-821-R-02-013, Appendix B, section 3.

Snedecor GW, Cochran WG (1980) \emph{Statistical Methods}, 7th edition. Iowa
State University Press.
}
