% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toxcalc-params.R
\name{toxcalc_params}
\alias{toxcalc_params}
\title{Shared parameter documentation}
\arguments{
\item{x}{A \code{toxcalc_data()} object, or a data frame that can be coerced to
one by passing it through \code{toxcalc_data()}.}

\item{data}{A data frame with one row per replicate test chamber.}

\item{conc}{Name of the column holding the exposure concentration. A string.}

\item{response}{Name of the column holding the response. For
\code{type = "continuous"} this is the measured value; for \code{type = "quantal"}
it is the number of organisms affected. A string.}

\item{n_exposed}{Name of the column holding the number of organisms exposed
in each replicate. Required when \code{type = "quantal"}, otherwise \code{NULL}.}

\item{replicate}{Name of an optional column labelling replicates within a
concentration, or \code{NULL}.}

\item{type}{The response type. Either \code{"continuous"} (growth, reproduction,
biomass) or \code{"quantal"} (survival, fertilisation, hatching).}

\item{direction}{The expected direction of the concentration-response
relationship for the measured response. Either \code{"decreasing"} (the usual
case, where toxicity reduces the response) or \code{"increasing"}.}

\item{control}{The concentration treated as the control. Defaults to \code{0}.}

\item{conc_units}{Units of \code{conc}, used only for labelling output. A string.}

\item{response_units}{Units of \code{response}, used only for labelling output.
A string, or \code{NULL}.}

\item{alpha}{Significance level for the hypothesis tests themselves. The EPA
manuals specify 0.05, one-sided.}

\item{alpha_assumption}{Significance level for the assumption tests
(normality and homogeneity of variance). The EPA manuals specify 0.01.}

\item{p}{The effect percentage or percentages to estimate, on a 1-99 scale.
For example \code{25} requests an IC25, and \code{50} an LC50.}

\item{ci_level}{Confidence level for interval estimates. Defaults to 0.95.}

\item{nboot}{Number of bootstrap resamples used for inhibition
concentration confidence limits.}

\item{seed}{Optional integer seed, set before any resampling so that results
are reproducible. \code{NULL} leaves the random number stream untouched.}

\item{...}{Additional arguments passed to \code{toxcalc_data()} when \code{x} is a
bare data frame, and otherwise unused.}
}
\description{
Documentation-only object holding the \verb{@param} tags shared across the
package. Every exported function inherits from it with
\verb{@inheritParams toxcalc_params}, so each argument is described in exactly
one place and cannot drift between topics.
}
\keyword{internal}
