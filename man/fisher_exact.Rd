% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nonparametric.R
\name{fisher_exact}
\alias{fisher_exact}
\title{Fisher's Exact Test}
\usage{
fisher_exact(x, ..., alpha = 0.05, adjust = c("none", "bonferroni"))
}
\arguments{
\item{x}{A \code{toxcalc_data()} object, or a data frame that can be coerced to
one by passing it through \code{toxcalc_data()}.}

\item{...}{Additional arguments passed to \code{toxcalc_data()} when \code{x} is a
bare data frame, and otherwise unused.}

\item{alpha}{Significance level for the hypothesis tests themselves. The EPA
manuals specify 0.05, one-sided.}

\item{adjust}{How to adjust for multiplicity. \code{"none"} is the EPA default
for this test.}
}
\value{
An object of class \code{toxcalc_comparison}, a list with elements
\code{test}, \code{method}, \code{comparisons} (a data frame with one row per treatment
concentration), \code{noec}, \code{loec}, \code{monotone}, \code{critical}, \code{sw}, \code{df},
\code{alpha} and \code{reference}.
}
\description{
Compares the proportion responding at each concentration with the control
proportion, using the hypergeometric distribution. The EPA manuals apply it
to survival, whose outcome is counted rather than measured.
}
\details{
Requires quantal data, so \code{toxcalc_data(type = "quantal")} with \code{n_exposed}
supplied.

\strong{No multiplicity adjustment is applied by default.} Section 1 of Appendix
G states that because Fisher's Exact Test is itself conservative, "a
pair-wise comparison error rate of 0.05 is suggested rather than an
experiment-wise error rate". Set \code{adjust} to depart from that.

The manual works the test by looking up a table of significance levels
(Table G.5). This package uses \code{\link[stats:fisher.test]{stats::fisher.test()}}, which computes the
same probability directly.
}
\examples{
# Appendix G worked example; the manual reports NOEC 12 and LOEC 25
fisher_exact(
  ceriodaphnia_g2,
  response = "dead",
  n_exposed = "exposed",
  type = "quantal"
)

}
\references{
US EPA (2002) EPA-821-R-02-013, Appendix G.
}
